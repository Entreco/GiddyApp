<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:dist="http://schemas.android.com/apk/distribution"
          xmlns:tools="http://schemas.android.com/tools"
          package="nl.entreco.giddyapp.signup">

    <dist:module
            dist:onDemand="false"
            dist:instant="false"
            dist:title="@string/title_signup">
        <dist:fusing dist:include="true"/>
    </dist:module>

    <application
            tools:ignore="AllowBackup,MissingApplicationIcon"
            android:name="nl.entreco.giddyapp.GiddyApp">
        <activity
                android:name=".SignupActivity"
                android:label="@string/title_signup"
                android:launchMode="singleTop"
                android:theme="@style/AppTheme.SignupTheme">

            <intent-filter android:autoVerify="true" android:order="1">

                <action android:name="android.intent.action.VIEW"/>

                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>

                <data android:scheme="https"/>
                <data android:host="giddy.entreco.nl"/>
                <data android:path="/email"/>

            </intent-filter>

            <!-- For Release -->
            <intent-filter android:autoVerify="true" android:order="2">

                <action android:name="android.intent.action.VIEW"/>

                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>

                <data android:scheme="https"/>
                <data android:host="giddy.page.link"/>

            </intent-filter>

            <!-- For Debug -->
            <intent-filter android:autoVerify="true" android:order="2">

                <action android:name="android.intent.action.VIEW"/>

                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>

                <data android:scheme="https"/>
                <data android:host="giddydev.page.link"/>

            </intent-filter>

        </activity>
    </application>
</manifest>

